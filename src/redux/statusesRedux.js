export const getAllStatuses = ({statuses}) => statuses;

//action creators
const reducer = (statePart = [], action) => {
  switch (action.type) {
    default:
      return statePart;
  }
};

export default reducer;